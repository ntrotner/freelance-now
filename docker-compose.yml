version: '3.3'
services:
  #----------  HTML/Fetch Services  -------
  freelancenow:
    build: ./
    container_name: freelancenow
    ports:
      - 8080:8080
    depends_on:
      - mongodb
    environment:
      - EXPRESSPORT=8080
      - EXPRESS_SESSION_SECRET=ADDSESSIONKEY
      - BID=
      - paypalUser=
      - paypalPass=

  #-----------      DATABASE     ----------
  mongodb:
    image: mongo:latest
    container_name: mongodb
    volumes:
      - "${GITLABRUNNERDB}/mongodbfreelance:/data/db"
